# The Ultimate Telegram Bot

Продвинутый Telegram-бот для транскрипции голосовых сообщений и видео-заметок с использованием OpenAI Whisper.

## Возможности

- 🎙️ Транскрипция голосовых сообщений
- 📹 Транскрипция видео-заметок (кружочки)
- 🚀 Асинхронная обработка с очередью
- 🔒 Режим отладки для ограничения доступа
- 📝 Интеллектуальное разделение длинных текстов
- 🔄 Поддержка CUDA для ускорения
- 🐳 Docker-контейнеризация
- 📊 Подробное логирование

## Требования

- Python 3.8+
- FFmpeg
- NVIDIA GPU (опционально, для ускорения)
- Docker и Docker Compose (для контейнеризации)

## Установка

### Локальная установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd The-Ultimate-TG-bot
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте переменные окружения:**
   ```bash
   cp example.env .env
   # Отредактируйте .env файл с вашими настройками
   ```

4. **Запустите бота:**
   ```bash
   python main.py
   ```

### Docker установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd The-Ultimate-TG-bot
   ```

2. **Настройте переменные окружения:**
   ```bash
   cp example.env .env
   # Отредактируйте .env файл с вашими настройками
   ```

3. **Запустите с Docker Compose:**
   ```bash
   docker-compose up -d
   ```

## Конфигурация

### Переменные окружения

| Переменная | Обязательная | Описание | Значение по умолчанию |
|-----------|-------------|----------|----------------------|
| `TELEGRAM_BOT_TOKEN` | Да | Токен Telegram бота | - |
| `DEBUG_CHAT_ID` | Нет | ID чата для отладки | - |
| `DEBUG_MODE` | Нет | Включить режим отладки | `False` |
| `LOG_LEVEL` | Нет | Уровень логирования | `INFO` |
| `MAX_MESSAGE_LENGTH` | Нет | Максимальная длина сообщения | `3696` |
| `VOICE_FOLDER` | Нет | Папка для голосовых файлов | `voice_messages` |
| `VIDEO_NOTE_FOLDER` | Нет | Папка для видео файлов | `video_notes` |

### Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен в переменную `TELEGRAM_BOT_TOKEN`

## Использование

### Основные команды

- **Голосовые сообщения** - отправьте голосовое сообщение боту
- **Видео-заметки** - отправьте видео-заметку (кружочек) боту
- `/check` - проверить очередь обработки
- `/everyone` - упомянуть всех администраторов в группе

### Режим отладки

Когда включен режим отладки (`DEBUG_MODE=True`), бот будет работать только в указанном чате (`DEBUG_CHAT_ID`). Это полезно для тестирования.

## Архитектура

### Компоненты

- **Config** - управление конфигурацией
- **ChatManager** - управление очередью обработки
- **VoiceBot** - основной класс бота
- **Logging** - система логирования с ротацией файлов

### Обработка сообщений

1. Получение голосового сообщения/видео-заметки
2. Скачивание и сохранение файла
3. Добавление в очередь обработки
4. Транскрипция с помощью Whisper
5. Разделение длинного текста на части
6. Отправка результата пользователю

## Мониторинг

### Логирование

Бот создает подробные логи с ротацией файлов:
- Размер файла лога: 10MB
- Количество архивных файлов: 5
- Уровни логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL

### Docker Health Check

При использовании Docker, контейнер автоматически проверяется каждые 30 секунд.

## Производительность

### Оптимизация GPU

При наличии NVIDIA GPU, бот автоматически использует CUDA для ускорения обработки.

### Управление памятью

- Docker контейнер ограничен 4GB RAM
- Автоматическая очистка временных файлов
- Оптимизированная обработка больших файлов

## Безопасность

- Запуск от непривилегированного пользователя
- Ограничения безопасности Docker
- Валидация входных данных
- Безопасная обработка ошибок

## Устранение неполадок

### Общие проблемы

1. **Ошибка "Model not found"**
   - Убедитесь, что у вас достаточно места на диске
   - Проверьте интернет-соединение для скачивания модели

2. **Ошибка "CUDA out of memory"**
   - Уменьшите размер модели или используйте CPU

3. **Бот не отвечает**
   - Проверьте токен бота
   - Убедитесь, что бот добавлен в чат

### Логи

Проверьте файл `bot.log` для подробной информации об ошибках.

## Разработка

### Структура проекта

```
├── main.py              # Основной код бота
├── requirements.txt     # Python зависимости
├── Dockerfile          # Docker конфигурация
├── docker-compose.yml  # Docker Compose конфигурация
├── example.env         # Пример переменных окружения
└── README.md           # Документация
```

### Тестирование

1. Создайте тестовый бот
2. Установите `DEBUG_MODE=True`
3. Укажите `DEBUG_CHAT_ID` для тестирования

## Лицензия

Этот проект распространяется под лицензией MIT.

## Поддержка

При возникновении проблем создайте issue в репозитории.
