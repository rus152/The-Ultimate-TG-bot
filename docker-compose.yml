version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    environment:
    
    command: ["./venv/bin/python", "main.py"]
    # Автоматическое удаление контейнера после завершения работы
    restart: "no"

volumes:
  venv:
